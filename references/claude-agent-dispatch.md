# Stage 4 Phase A — live probe evidence (recorded 2026-08-19)

Environment: `claude 2.1.235`, scratch plugin at `atlas-probe-plugin/` (`.claude-plugin/plugin.json` with only `name`+`description`, no `agents` field; role files at plugin-root `agents/*.md`, unchanged from kimi-atlas's own layout convention). Invoked as `claude --plugin-dir <scratch> --permission-mode bypassPermissions -p "<prompt>"`.

## Probe: dispatch determinism + plugin-root `agents/` auto-discovery

3 trials, 7 total dispatches, alternating target and order:

| Trial | Sequence | Result |
|---|---|---|
| 1 | a, b | `PROBE_RESPONSE=A`, `PROBE_RESPONSE=B` — both correct |
| 2 | b, a (reversed order) | `PROBE_RESPONSE=B`, `PROBE_RESPONSE=A` — both correct |
| 3 | a, b, a (repeat same type twice) | `PROBE_RESPONSE=A`, `PROBE_RESPONSE=B`, `PROBE_RESPONSE=A` — all correct, each with a distinct `agentId`, confirming independent instances |

**Result: 7/7 correct routing. PASS.** No manifest `agents` field was declared — plugin-root `agents/*.md` was auto-discovered exactly as kimi-atlas's own existing layout already assumes. No relocation needed.

## Probe: `prompt` parameter fidelity at size

A 15,882-byte nested JSON payload (25 success-criteria strings, a 40-node/39-edge synthetic ContextGraph, unicode text in 3 scripts, embedded `\n`/`\t` escapes, HTML-special characters) was embedded in a dispatch to a `probe-fidelity` subagent instructed to write everything between two markers verbatim to a file via the `Write` tool.

```
$ diff fidelity_payload.json fidelity_received.json && echo "IDENTICAL - byte-for-byte match"
IDENTICAL - byte-for-byte match
$ md5sum fidelity_payload.json fidelity_received.json
03955886c1921a56f8160cf0911bcb7c  fidelity_payload.json
03955886c1921a56f8160cf0911bcb7c  fidelity_received.json
```

Payload composition (generated by a Python script, `run_id: probe-fidelity-9f3a2c1e`): 25 `success_criteria` strings, a synthetic `context_graph` with 40 `nodes` / 39 sequential `edges`, and a `unicode_stress` field mixing Japanese text, emoji, and HTML-special characters — chosen to mirror the shape of a real kimi-atlas ContextGraph packet.

**Result: byte-for-byte identical at 15.8 KB — nearly 8x the ≥2KB target. PASS.**

## Conclusion

Both of Stage 4's remaining Phase A unknowns are resolved with direct evidence, not inference. Stage 4 Phase B/C (rewriting the 7 role files and both SKILL.md dispatch sections) can proceed once Stage 1 and Stage 3 are actually implemented — this probe only had to prove the *mechanism* works, which it does.
