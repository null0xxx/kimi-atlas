# Track A — historical archive

This branch preserves the original **Kimi_subagents / Track A No-Patch Overlay** work
(under `track-a/`), including all execution/inventory-sync reports. It is kept off `main`
per PLAN.md OD-2: the manual index-sync workflow is replaced by `scripts/inventory_drift.py`.
Nothing here is loaded by the kimi-atlas plugin; it is history only.
