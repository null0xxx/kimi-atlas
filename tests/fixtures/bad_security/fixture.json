{
  "intent": "Add a verify_token(provided, expected) function that returns whether a provided API request token matches the expected secret token used to authenticate the caller.",
  "success_criteria": [
    "verify_token returns True when the provided token equals the expected secret token",
    "verify_token returns False when the provided token differs from the expected secret token",
    "verify_token returns False when either token is empty"
  ],
  "verify_cmd": "python3 -m unittest test_tokenauth",
  "scope_paths": ["tokenauth.py", "test_tokenauth.py"],
  "expected_verdict": "UNVERIFIED",
  "expected_lens": "SECURITY"
}
